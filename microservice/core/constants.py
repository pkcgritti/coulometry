DEFAULT_EORDER = 20 # Tamanho de pontos utilizados para extensão do sinal
DEFAULT_EREPTS = 1  # Número de repetições da operação de extensão do sinal
DEFAULT_FORDER = 20 # Tamanho janela de filtro simétrico (order * 2 + 1)
DEFAULT_WORDER = 5 # Tamanho janela de convolução (order * 2 + 1)
DEFAULT_OFFSET = DEFAULT_EORDER * DEFAULT_EREPTS # Janela para procura de ponto mínimo
